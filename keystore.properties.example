# Release signing config. Copy to `keystore.properties` and fill in.
# `keystore.properties` and the *.jks/*.keystore file are gitignored — never commit them.
storeFile=release-keystore.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=geofence
keyPassword=YOUR_KEY_PASSWORD
